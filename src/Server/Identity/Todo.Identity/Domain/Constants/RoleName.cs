namespace Todo.Identity.Domain.Constants;

public static class RoleName
{
    public const string User = nameof(User);
    public const string Admin = nameof(Admin);
    public const string Support = nameof(Support);
}
